export const LANDING_REVIEW = {
  owner: "ShobhitPatra",
  repo: "hawkeye",
  number: 87,
  title: "feat(web): show a reviewing line in the living review from claim time",
  branch: "feat/web-reviewing-line",
  headSha: "989dcb7",
  runner: "shobhit-fedora",
  url: "https://github.com/ShobhitPatra/hawkeye/pull/87",
  terminal: [
    { at: "05:57:40", state: "claimed", detail: "ShobhitPatra/hawkeye#87 at 989dcb7", delayMs: 0 },
    {
      at: "05:57:46",
      state: "reviewing",
      detail: "~/.cache/hawkeye/runs/20260906-055740-ShobhitPatra-hawkeye-87",
      delayMs: 400,
    },
    { at: "06:00:28", state: "posted", detail: "", delayMs: 2400 },
  ],
  posted: { verdict: "Blocked", findings: 4, mustFix: 1, turns: 6, duration: "2m 48s" },
  bodyHtml:
    '<h3>Blocked</h3>\n<ul>\n<li>Round one through the placeholder with any line-anchored finding records the supplemental review as the living review, so round two patches the wrong comment.</li>\n<li>The early superseded/already-posted returns in postReviewForRun never clear the placeholder, leaving a permanently shimmering &quot;Reviewing on&quot; comment.</li>\n<li>Otherwise well-scoped: escaped badge, additive migration, docs in lockstep, good unit coverage of the block and the mark/clear paths.</li>\n</ul>\n<h4>Must fix</h4>\n<ul>\n<li><p><strong>Round one via the placeholder records the supplemental review id as the living review.</strong> <code>apps/web/src/review-posting.ts:292</code>\nWith inline comments, <code>record(supplemental.id)</code> runs and the body is written to the placeholder, but this is the only reviewPosted row, so <code>livingReviewFor</code> returns the supplemental review. Round two then prepends the reviewing line to that marker-only comment and overwrites it, stranding round one&#39;s body in the placeholder. <code>59b565718f8d</code></p>\n<details><summary>why</summary>\n\n<p>livingReviewFor orders by postedAt asc; before this PR round one went through the postRenderedReview branch and recorded the id of the review that actually holds the body. The placeholder branch breaks that invariant only when comments.length &gt; 0, which is the common case for a first review with an anchored finding.</p>\n</details></li>\n</ul>\n<h4>Should fix</h4>\n<ul>\n<li><strong>The in-transaction superseded and already-posted returns never close the placeholder.</strong> <code>apps/web/src/review-posting.ts:185</code>\nA newer job that finishes first makes this return <code>superseded</code> before <code>living</code>/<code>placeholder</code> is resolved, so the round-one placeholder keeps the animated &quot;Reviewing on&quot; badge forever. Only the later supersede check (after the token fetch) clears it, and that is the only path tested. <code>ca837f2d66b5</code></li>\n</ul>\n<h4>Optional</h4>\n<ul>\n<li><strong>Failed or superseded rounds accumulate dead placeholder comments on the pull request.</strong> <code>apps/web/src/runner-api.ts:197</code>\nA closed placeholder never becomes a living review, so the next claim posts a fresh one; three failed rounds leave three &quot;The review did not complete.&quot; comments. Reusing the closed placeholder, or minimizing it, keeps the thread to one. <code>88ec8597af01</code></li>\n<li><strong>The route test is not colocated with the route it tests.</strong> <code>apps/web/src/reviewing-route.test.ts:2</code>\nAGENTS.md asks for tests beside the module; this one sits in <code>src/</code> because the vitest include glob is <code>apps/*/src/**</code>. Widening the glob would let it live next to <code>app/status/reviewing/route.ts</code>. <code>b9e6dff28126</code></li>\n</ul>\n<details>\n<summary>Review lenses</summary>\n\n<table>\n<thead>\n<tr>\n<th>Lens</th>\n<th>Assessment</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Intent</td>\n<td>The diff does what the body says: a marked block at claim time, a placeholder review on round one, closing sentences on failure and supersede, and the badge route. No unrelated work; the vitest <code>@/</code> alias and the added <code>review</code> client method are both needed by the feature.</td>\n</tr>\n<tr>\n<td>Behavior</td>\n<td>The happy path holds, but two placeholder paths do not. When round one renders inline comments the reviewPosted row keeps the supplemental id while the body goes into the placeholder, so the living review pointer is wrong from round two on. The transaction&#39;s early <code>superseded</code>/<code>already-posted</code> returns skip clearing entirely.</td>\n</tr>\n<tr>\n<td>Blast radius</td>\n<td>Migration 0008 is a nullable column, additive. <code>GitHubClient.review</code> is a new required interface method (every mock updated) and <code>claimJob</code> gains a required <code>controlPlaneUrl</code>. The new <code>/status/reviewing</code> route is public and unauthenticated but only echoes a length-capped, XML-escaped query string, so no injection surface.</td>\n</tr>\n<tr>\n<td>Verification</td>\n<td>Good coverage of the block, escaping, mark/clear branches, the route headers and the claim-time placeholder. The gap is exactly where the bugs are: no test posts a round-one placeholder review that also has inline comments, and none exercises the pre-GitHub superseded return.</td>\n</tr>\n<tr>\n<td>Fit</td>\n<td>Naming, module shape, dependency-free SVG and error-swallow-with-log match the repo. <code>reviewing-route.test.ts</code> lives in <code>src/</code> while its subject is <code>app/status/reviewing/route.ts</code>, which bends the colocation rule; no comments were added and formatting is machine-owned.</td>\n</tr>\n<tr>\n<td>Hygiene</td>\n<td>Single conventional <code>feat(web):</code> subject, no trailers. README and the design SKILL.md are updated in the same PR, and the follow-up (stale-job sweep leaving a line) is named rather than smuggled in. No secrets or new dependencies.</td>\n</tr>\n</tbody></table>\n</details>\n\n<hr>\n<p>Reviewed by <a href="https://github.com/ShobhitPatra/hawkeye">Hawkeye</a> on the author&#39;s own plan \u00b7 round 1 \u00b7 6 turns</p>',
} as const;
